# Dependencies — di-install ulang otomatis lewat `npm install`, tidak perlu di-push
node_modules/

# OS junk files
.DS_Store
Thumbs.db

# NOTE: folder dist/ SENGAJA TIDAK di-ignore.
# Karena build dilakukan manual di komputer sendiri (bukan di server Vercel),
# hasil build (dist/) harus ikut di-push ke GitHub supaya Vercel bisa langsung
# menyajikannya sebagai Output Directory tanpa perlu build command apa pun.
